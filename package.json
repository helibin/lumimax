{
  "name": "lumimax",
  "version": "0.1.0",
  "private": true,
  "description": "Lumimax monorepo root: orchestrates api/ (NestJS micro-services) and web/ (Vben Admin + www).",
  "packageManager": "pnpm@10.16.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "_comment": "顶层只做编排：不在根目录做 pnpm install，而是分别进入 api/ 和 web/ 执行各自的 workspace 命令，避免 catalog 版本冲突。",
    "install:all": "pnpm --dir api install && pnpm --dir web install",
    "dev:api": "pnpm --dir api dev",
    "dev:gateway": "pnpm --dir api dev:gateway",
    "dev:base": "pnpm --dir api dev:base",
    "dev:biz": "pnpm --dir api dev:biz",
    "dev:web:admin": "pnpm --dir web --filter @lumimax/admin dev",
    "dev:web:www": "pnpm --dir web --filter @lumimax/www dev",
    "build:api": "pnpm --dir api build",
    "build:web:admin": "pnpm --dir web build:admin",
    "build:web:www": "pnpm --dir web build:www",
    "build:all": "pnpm run build:api && pnpm run build:web:admin && pnpm run build:web:www",
    "db:setup": "pnpm --dir api db:setup",
    "db:migrate": "pnpm --dir api db:migrate",
    "db:seed": "pnpm --dir api db:seed",
    "docker:build": "docker build -f docker/Dockerfile -t lumimax:latest .",
    "docker:run": "sh -lc 'if [ ! -f .env ]; then cp docker/.env.example .env && echo \"[docker:run] created .env from docker/.env.example\"; fi; if ! docker image inspect lumimax:latest >/dev/null 2>&1; then echo \"[docker:run] image not found, building lumimax:latest ...\"; docker build -f docker/Dockerfile -t lumimax:latest .; fi; docker run --rm -p 8080:80 --env-file .env --name lumimax lumimax:latest'",
    "compose:up": "docker compose -f compose.stack.yml up -d --build",
    "compose:down": "docker compose -f compose.stack.yml down",
    "compose:logs": "docker compose -f compose.stack.yml logs -f"
  },
  "keywords": [
    "lumimax",
    "monorepo",
    "nestjs",
    "vue",
    "vben-admin",
    "iot"
  ]
}
